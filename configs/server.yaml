listen_addr: "0.0.0.0"
listen_port: 8443
log_level: "info"
max_connections: 100
ca_service_url: "${CA_SERVICE_URL}"
cert_cache_dir: "${CERT_CACHE_DIR:-/tmp/fluidity-certs}"
ca_cert_file: "${CA_CERT_FILE}"
